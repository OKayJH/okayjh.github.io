# 调试记录与常见问题

## 摘要
本文件记录站点日常维护时最常见的定位入口，重点覆盖浏览器页签标题、首页作者介绍、标签和站点短描述的修改路径，便于后续最小范围改动。

## 常见问题表
| 问题 | 现象 | 关键文件 | 结论 | 建议 |
| --- | --- | --- | --- | --- |
| 浏览器页签标题在哪里改 | 浏览器标签页显示的站点名或页面名不符合预期 | `src/pages/index.astro` `src/layouts/Layout.astro` | 首页自定义标题由首页传入，通用后缀由布局统一处理 | 先改页面 `title`，再看布局里的标题规范化逻辑 |
| 浏览器左上角小图标在哪里改 | 浏览器标签页前面的站点小图标不符合预期 | `public/favicon-okay-logo.png` `src/layouts/Layout.astro` | 图标文件在 `public/`，布局层负责引用 | 优先替换图标文件并确认 `<link rel="icon">` 指向正确 |
| 默认站点描述在哪里改 | 页面未单独传 `description` 时，描述仍显示旧定位 | `src/layouts/Layout.astro` | `defaultDescription` 是全站默认描述入口 | 统一站点定位时优先改这里 |
| 首页作者介绍在哪里改 | 首页首屏文案、弹窗介绍和标签与个人定位不一致 | `src/components/AuthorProfile.astro` | Hero 文案、弹窗文案和标签都在同一组件维护 | 优先在此文件原地更新，不要散落到其他文件 |
| 页脚站点短说明在哪里改 | 页脚仍显示旧站点名称或旧标语 | `src/components/Footer.astro` | 页脚文案单独维护 | 站点整体定位变化时应同步更新 |
| 构建失败但文案改动无报错 | 运行 `npm run build` 报 `NoAdapterInstalled` | `astro.config.mjs` `package.json` | 属于仓库现有部署配置问题，不是首页文案修改引入 | 后续单独梳理 API 路由与生产适配器 |

## 调试命令表
| 命令 | 执行位置 | 用途 | 示例 | 风险 |
| --- | --- | --- | --- | --- |
| `npm run build` | 宿主机项目根目录 | 验证 Astro 构建链路 | `npm run build` | 当前会因缺少生产适配器失败 |
| `npm run dev` | 宿主机项目根目录 | 本地查看首页文案和页签效果 | `npm run dev` | 长驻进程，重复开启前要确认已有实例 |

## 本次调试记录
| 日期 | 现象 | 处理动作 | 结果 |
| --- | --- | --- | --- |
| 2026-03-12 | 首页作者介绍仍包含 SoC 和芯片型号导向文案，浏览器标题过于通用 | 更新首页标题、默认描述、作者介绍、标签和页脚短说明 | 文案已统一到“智能硬件、嵌入式系统、电子产品设计”方向 |
| 2026-03-12 | 需要最小验证改动是否引入错误 | 读取已改文件 IDE 诊断，并执行 `npm run build` | IDE 诊断无错误；构建被仓库现有适配器问题阻塞 |
| 2026-03-12 | 浏览器页签左侧图标需要换成新的品牌标识 | 将用户提供的 OKay 标识图做去背景处理，生成新的 `favicon-okay-logo.png` 并更新布局引用 | 图标已切换到新的 OKay favicon |
| 2026-03-12 | 新生成的 OKay favicon 显示不完整 | 改用完整图形版 logo 源图重建同名 `favicon-okay-logo.png` | 图标主体已完整保留 |

## 变更记录
| 日期 | 修改人/来源 | 修改原因 | 影响范围 | 对应功能/文件/模块 |
| --- | --- | --- | --- | --- |
| 2026-03-12 | Cursor Agent | 项目缺少常见维护入口说明，补充首页文案与标题的排查路径 | 首页、布局、页脚 | `src/components/AuthorProfile.astro` `src/layouts/Layout.astro` `src/components/Footer.astro` |
| 2026-03-12 | Cursor Agent | 补充浏览器页签图标的定位入口与本次 OKay 标识替换记录 | 布局层、静态资源 | `public/favicon-okay-logo.png` `src/layouts/Layout.astro` |
| 2026-03-12 | Cursor Agent | 追加记录完整图形版 OKay favicon 的修正过程 | 布局层、静态资源 | `public/favicon-okay-logo.png` |
